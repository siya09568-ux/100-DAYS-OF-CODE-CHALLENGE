/*Q7: Write a program to swap two numbers without using a third variable.*/

#include <stdio.h>
int main() { 
	int a,b;
	printf("enter a,b :");
	scanf("%d" "%d", &a,&b);
	
	printf("after swap %d %d", b,a);
	return 0;
}
